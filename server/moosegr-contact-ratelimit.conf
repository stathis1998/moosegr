# Rate limit for the contact form endpoint (zone must be declared at
# http level). Installed at /etc/nginx/conf.d/moosegr-contact-ratelimit.conf
limit_req_zone $binary_remote_addr zone=contact_form:1m rate=3r/m;
